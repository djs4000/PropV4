#pragma once

// Default WiFi credentials used on first boot. These can be overridden at runtime
// via the Preferences store and web UI. Values are placeholders and should be
// replaced with deployment-specific credentials before field use.
static constexpr const char *DEFAULT_WIFI_SSID = "Prop";
static constexpr const char *DEFAULT_WIFI_PASS = "";
